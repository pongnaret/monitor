# 📥 คู่มือติดตั้ง Agent สำหรับเครื่องลูกข่าย

คู่มือนี้สำหรับติดตั้ง Agent บนเครื่องคอมพิวเตอร์ที่ต้องการให้ Admin monitor

---

## 📋 สิ่งที่ต้องเตรียม

1. ไฟล์ที่ต้องคัดลอกไปยังเครื่องลูกข่าย:
   - `agent.py`
   - `config.py`
   - `requirements.txt`

2. ข้อมูล Server:
   - IP Address ของเครื่อง Server (เช่น `10.31.112.155`)
   - Port ของ Server (ค่าเริ่มต้น: `8000`)

---

## 🚀 ขั้นตอนการติดตั้ง

### **ขั้นตอนที่ 1: ติดตั้ง Python**

#### สำหรับ Windows:

1. ดาวน์โหลด Python จาก: https://www.python.org/downloads/
2. เลือก Python 3.8 ขึ้นไป (แนะนำ 3.11 หรือใหม่กว่า)
3. เปิดไฟล์ติดตั้ง
4. **⚠️ สำคัญมาก: ติ๊กถูก "Add Python to PATH"**
5. คลิก "Install Now"
6. รอจนติดตั้งเสร็จ

#### ทดสอบว่าติดตั้งสำเร็จ:

เปิด Command Prompt หรือ PowerShell แล้วพิมพ์:

```bash
python --version
```

หรือ

```bash
py --version
```

ถ้าแสดงเวอร์ชัน Python แปลว่าติดตั้งสำเร็จ ✅

---

### **ขั้นตอนที่ 2: คัดลอกไฟล์**

สร้างโฟลเดอร์สำหรับ Agent (เช่น `C:\MonitorAgent`) และคัดลอกไฟล์เหล่านี้เข้าไป:

```
C:\MonitorAgent\
├── agent.py
├── config.py
└── requirements.txt
```

---

### **ขั้นตอนที่ 3: แก้ไขค่า Config**

เปิดไฟล์ `config.py` ด้วย Notepad หรือ Text Editor แล้วแก้ไขบรรทัดนี้:

```python
SERVER_URL = "http://10.31.112.155:8000"
```

**เปลี่ยน `10.31.112.155` เป็น IP Address ของเครื่อง Server**

บันทึกไฟล์

---

### **ขั้นตอนที่ 4: ติดตั้ง Python Libraries**

เปิด Command Prompt หรือ PowerShell ในโฟลเดอร์ที่เก็บไฟล์:

```bash
cd C:\MonitorAgent
```

ติดตั้ง libraries:

**วิธีที่ 1 (แนะนำ):**
```bash
python -m pip install -r requirements.txt
```

**วิธีที่ 2:**
```bash
py -m pip install -r requirements.txt
```

รอจนติดตั้งเสร็จ (ประมาณ 1-2 นาที)

---

### **ขั้นตอนที่ 5: ทดสอบ Agent**

รันคำสั่ง:

```bash
python agent.py
```

หรือ

```bash
py agent.py
```

ถ้าเห็นข้อความนี้แปลว่าสำเร็จ:

```
✅ ลงทะเบียนสำเร็จ: [ชื่อเครื่อง] (ID: 1)
🚀 เริ่มต้น Monitoring Agent
📡 Server: http://10.31.112.155:8000
🖥️  เครื่อง: [ชื่อเครื่อง]
⏱️  อัพเดตทุก 30 วินาที
```

---

## 🔄 การตั้งค่าให้ Agent รันอัตโนมัติเมื่อเปิดเครื่อง

### วิธีที่ 1: ใช้ Task Scheduler (Windows)

1. เปิด "Task Scheduler"
2. คลิก "Create Basic Task"
3. ตั้งชื่อ: "Monitor Agent"
4. Trigger: "When the computer starts"
5. Action: "Start a program"
6. Program/script: `C:\Users\[User]\AppData\Local\Programs\Python\Python3xx\python.exe`
7. Arguments: `C:\MonitorAgent\agent.py`
8. Start in: `C:\MonitorAgent`
9. เสร็จ!

### วิธีที่ 2: สร้าง Batch File

สร้างไฟล์ `start_agent.bat` ในโฟลเดอร์เดียวกัน:

```batch
@echo off
cd /d C:\MonitorAgent
python agent.py
pause
```

วางไฟล์นี้ใน Startup folder:
- กด `Win + R`
- พิมพ์: `shell:startup`
- วาง `start_agent.bat` เข้าไป

---

## 🛠️ การแก้ปัญหา

### ❌ Agent เชื่อมต่อ Server ไม่ได้

**ปัญหา:**
```
❌ ไม่สามารถเชื่อมต่อ server: ...
```

**วิธีแก้:**

1. **ตรวจสอบ Server URL ใน `config.py`**
   - ต้องเป็น IP ของเครื่อง Server
   - ต้องมี `http://` ข้างหน้า
   - ตัวอย่าง: `http://10.31.112.155:8000`

2. **ตรวจสอบว่า Server กำลังทำงานอยู่**
   - บนเครื่อง Server รันคำสั่ง: `python server.py`
   - เปิด Browser: `http://localhost:8000`

3. **ตรวจสอบ Network**
   - Ping เครื่อง Server: `ping 10.31.112.155`
   - ถ้า ping ไม่ผ่าน = ปัญหาเครือข่าย

4. **ตรวจสอบ Firewall**
   - Windows Firewall อาจบล็อก port 8000
   - เปิด Windows Defender Firewall
   - เพิ่ม Inbound Rule สำหรับ port 8000

---

### ❌ Python ไม่ทำงาน

**ปัญหา:**
```
'python' is not recognized...
```

**วิธีแก้:**

1. ใช้คำสั่ง `py` แทน `python`
2. หรือติดตั้ง Python ใหม่และติ๊กถูก "Add to PATH"

---

### ❌ Libraries ติดตั้งไม่ได้

**ปัญหา:**
```
pip : The term 'pip' is not recognized...
```

**วิธีแก้:**

ใช้คำสั่ง:
```bash
python -m pip install -r requirements.txt
```

หรือ

```bash
py -m pip install -r requirements.txt
```

---

## 💬 การใช้งาน Chat

### โหมด Monitor (ปกติ)

```bash
python agent.py
```

- ส่งข้อมูล monitoring อัตโนมัติ
- รับข้อความจาก Admin แบบอัตโนมัติ
- แจ้งเตือนเมื่อเกินค่ากำหนด

### โหมด Chat (แชทกับ Admin)

```bash
python agent.py chat
```

- พิมพ์ข้อความส่งหา Admin
- พิมพ์ `exit` เพื่อออก

---

## 📊 การตรวจสอบสถานะ

เมื่อ Agent ทำงาน คุณจะเห็น:

```
📊 ส่งข้อมูล metrics สำเร็จ - CPU: 45.2% | RAM: 62.8%
💬 ข้อความจาก Admin: สวัสดีครับ
```

ถ้ามีปัญหา Agent จะแจ้งเตือนอัตโนมัติ:

```
⚠️ CPU สูงเกินกำหนด: 95.3%
💬 ส่งข้อความ: ⚠️ CPU สูงเกินกำหนด: 95.3%
```

---

## 🔧 การหยุดและลบ Agent

### หยุดชั่วคราว

กด `Ctrl + C` ใน Command Prompt ที่รัน Agent

### ลบถาวร

1. ปิด Agent
2. ลบไฟล์ทั้งหมดในโฟลเดอร์
3. ถ้าตั้ง Task Scheduler ให้ลบ Task ออก

---

## 📞 ติดต่อ Admin

หากมีปัญหาการติดตั้ง:

1. ใช้ Chat ใน Agent: `python agent.py chat`
2. หรือติดต่อ Admin โดยตรง

---

## ✅ Checklist การติดตั้ง

- [ ] ติดตั้ง Python เรียบร้อย
- [ ] คัดลอกไฟล์ครบทั้ง 3 ไฟล์
- [ ] แก้ไข `SERVER_URL` ใน `config.py`
- [ ] ติดตั้ง libraries ด้วย `pip install -r requirements.txt`
- [ ] ทดสอบรัน `python agent.py` สำเร็จ
- [ ] เห็นข้อความ "ลงทะเบียนสำเร็จ"
- [ ] ตรวจสอบใน Dashboard ของ Admin เห็นเครื่องปรากฏ

---

**เสร็จเรียบร้อย! 🎉**

ตอนนี้เครื่องคุณกำลังถูก Monitor โดย Admin แล้ว
