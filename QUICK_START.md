# ⚡ Quick Start Guide - ติดตั้งด่วน

## 🎯 สำหรับเครื่องลูกข่าย (Agent)

### ขั้นตอนติดตั้ง 3 ขั้นตอน:

**1. ติดตั้ง Python:**
- ดาวน์โหลด: https://www.python.org/downloads/
- ติ๊กถูก "Add Python to PATH"
- ติดตั้ง

**2. คัดลอกไฟล์:**
- แตก ZIP ไปที่ `C:\MonitorAgent`
- แก้ไข `config.py` บรรทัดที่ 4:
  ```python
  SERVER_URL = "http://10.31.112.155:8000"  # <-- IP ของ Server
  ```

**3. ติดตั้งและรัน:**
- ดับเบิลคลิก `install_agent.bat`
- ดับเบิลคลิก `start_agent.bat`

**เสร็จแล้ว! ✅**

---

## 🖥️ สำหรับ Admin (Server)

### เริ่มใช้งาน Server:

**1. ติดตั้ง (ครั้งแรกเท่านั้น):**
```bash
py -m pip install -r requirements.txt
py database.py
```

**2. เริ่ม Server:**
```bash
py server.py
```

**3. เปิด Dashboard:**
- Browser: http://localhost:8000

---

## 💬 การใช้งาน Chat

### จาก Admin:
1. เปิด Dashboard
2. คลิก "💬 Chat" ที่การ์ดเครื่อง
3. พิมพ์ข้อความและส่ง

### จากเครื่องลูกข่าย:
```bash
py agent.py chat
```
พิมพ์ข้อความและกด Enter

---

## 🔍 ตรวจสอบว่าทำงานหรือไม่

### บนเครื่องลูกข่าย:
เห็นข้อความนี้ = สำเร็จ ✅
```
✅ ลงทะเบียนสำเร็จ: [ชื่อเครื่อง]
📊 ส่งข้อมูล metrics สำเร็จ - CPU: 45% | RAM: 60%
```

### บน Dashboard:
- เห็นการ์ดเครื่องปรากฏ
- สถานะแสดง "Online" สีเขียว
- แสดงกราฟ CPU, RAM, Disk

---

## 🆘 แก้ปัญหาด่วน

**เชื่อมต่อไม่ได้:**
```bash
ping 10.31.112.155
```
ถ้า ping ไม่ผ่าน = ปัญหา Network

**Python ไม่ทำงาน:**
ใช้ `py` แทน `python`

**ต้องการความช่วยเหลือ:**
อ่าน `INSTALL_AGENT.md` ฉบับเต็ม

---

**หมายเหตุ:**
- เครื่อง Server ต้องเปิดอยู่ตลอดเวลา
- Agent ส่งข้อมูลทุก 30 วินาที
- แจ้งเตือนอัตโนมัติเมื่อ CPU/RAM/Disk > 90%
