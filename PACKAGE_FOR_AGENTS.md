# 📦 วิธีสร้าง Package สำหรับแจกจ่ายไปยังเครื่องลูกข่าย

## 🎯 ไฟล์ที่ต้องแจกจ่ายให้เครื่องลูกข่าย

สร้างโฟลเดอร์ชื่อ `MonitorAgent` และคัดลอกไฟล์เหล่านี้เข้าไป:

### ไฟล์หลัก (จำเป็น):
```
MonitorAgent/
├── agent.py                      # โปรแกรม Agent
├── config_agent_template.py      # ไฟล์ config แบบ template
├── requirements.txt              # Python libraries
├── install_agent.bat             # สคริปต์ติดตั้งอัตโนมัติ
├── start_agent.bat               # สคริปต์เริ่มใช้งาน
└── INSTALL_AGENT.md              # คู่มือการติดตั้ง
```

---

## 📋 ขั้นตอนการเตรียม Package

### 1. สร้างโฟลเดอร์ MonitorAgent

```bash
mkdir MonitorAgent
```

### 2. คัดลอกไฟล์เข้าไป

คัดลอกไฟล์เหล่านี้จากโฟลเดอร์หลัก:

- `agent.py`
- `requirements.txt`
- `install_agent.bat`
- `start_agent.bat`
- `INSTALL_AGENT.md`

### 3. เปลี่ยนชื่อไฟล์ config

```bash
copy config_agent_template.py MonitorAgent\config.py
```

**แก้ไข `MonitorAgent\config.py` ให้ SERVER_URL ตรงกับ IP ของเครื่อง Server:**

```python
SERVER_URL = "http://10.31.112.155:8000"  # เปลี่ยนเป็น IP ของ Server
```

### 4. สร้างไฟล์ ZIP

คลิกขวาที่โฟลเดอร์ `MonitorAgent` → Send to → Compressed (zipped) folder

ได้ไฟล์: `MonitorAgent.zip`

---

## 🚀 วิธีแจกจ่ายและติดตั้งบนเครื่องลูกข่าย

### สำหรับผู้ติดตั้ง (IT/Admin):

1. **แชร์ไฟล์ `MonitorAgent.zip` ไปยังเครื่องลูกข่าย** ผ่าน:
   - Network Share
   - USB Drive
   - Email
   - Cloud Storage

2. **บนเครื่องลูกข่าย:**
   - แตกไฟล์ ZIP ไปที่ `C:\MonitorAgent`
   - ดับเบิลคลิก `install_agent.bat`
   - รอติดตั้ง Python libraries
   - ดับเบิลคลิก `start_agent.bat` เพื่อทดสอบ

3. **ตรวจสอบการทำงาน:**
   - เปิด Dashboard บนเครื่อง Admin
   - ดูว่าเครื่องลูกข่ายปรากฏในรายการหรือไม่

---

## 📝 สคริปต์ติดตั้งหลายเครื่องพร้อมกัน

### PowerShell Script สำหรับติดตั้งหลายเครื่อง (ผ่าน Network)

สร้างไฟล์ `deploy_to_all.ps1`:

```powershell
# รายชื่อเครื่องที่ต้องการติดตั้ง
$computers = @(
    "PC001",
    "PC002",
    "PC003"
)

$sourcePath = "\\SERVER\Share\MonitorAgent"
$destPath = "C:\MonitorAgent"

foreach ($computer in $computers) {
    Write-Host "กำลังติดตั้งบน $computer..." -ForegroundColor Cyan

    try {
        # สร้างโฟลเดอร์
        Invoke-Command -ComputerName $computer -ScriptBlock {
            New-Item -Path "C:\MonitorAgent" -ItemType Directory -Force
        }

        # คัดลอกไฟล์
        Copy-Item -Path "$sourcePath\*" -Destination "\\$computer\C$\MonitorAgent" -Recurse -Force

        # รันติดตั้ง
        Invoke-Command -ComputerName $computer -ScriptBlock {
            Set-Location C:\MonitorAgent
            python -m pip install -r requirements.txt
        }

        Write-Host "✅ ติดตั้งสำเร็จบน $computer" -ForegroundColor Green
    }
    catch {
        Write-Host "❌ ติดตั้งล้มเหลวบน $computer : $_" -ForegroundColor Red
    }
}
```

---

## 🔧 การตั้งค่าเพิ่มเติม

### ตั้งค่าให้ Agent รันอัตโนมัติเมื่อเปิดเครื่อง

สร้างไฟล์ `setup_autostart.bat`:

```batch
@echo off
echo กำลังตั้งค่า Auto-start...

REM Get current directory
set AGENT_DIR=%cd%

REM Create Task Scheduler task
schtasks /create /tn "MonitorAgent" /tr "%AGENT_DIR%\start_agent.bat" /sc onstart /ru SYSTEM /f

if %errorlevel% equ 0 (
    echo ✅ ตั้งค่า Auto-start สำเร็จ
    echo Agent จะเริ่มทำงานอัตโนมัติเมื่อเปิดเครื่อง
) else (
    echo ❌ ตั้งค่าล้มเหลว กรุณารันด้วยสิทธิ์ Administrator
)

pause
```

---

## 📊 ตัวอย่าง Network Deployment

### ติดตั้งผ่าน Group Policy (Domain Environment):

1. **วาง Package ใน Network Share:**
   ```
   \\DC01\SYSVOL\domain.com\scripts\MonitorAgent\
   ```

2. **สร้าง GPO:**
   - Computer Configuration → Policies → Windows Settings
   - Scripts → Startup
   - Add: `\\DC01\SYSVOL\domain.com\scripts\MonitorAgent\install_agent.bat`

3. **Link GPO ไปยัง OU ที่ต้องการ**

4. **รัน `gpupdate /force` บนเครื่องลูกข่าย**

---

## ✅ Checklist สำหรับ IT Admin

### ก่อนแจกจ่าย:
- [ ] Server กำลังทำงานอยู่
- [ ] ทดสอบ Agent บนเครื่องทดสอบสำเร็จ
- [ ] แก้ไข `SERVER_URL` ใน config.py ให้ถูกต้อง
- [ ] สร้าง ZIP file ครบถ้วน
- [ ] เตรียมคู่มือการติดตั้ง

### ระหว่างติดตั้ง:
- [ ] Python ติดตั้งแล้วบนเครื่องลูกข่าย
- [ ] Network เชื่อมต่อ Server ได้ (ping ผ่าน)
- [ ] Firewall เปิด port 8000
- [ ] Agent รันได้และลงทะเบียนสำเร็จ

### หลังติดตั้ง:
- [ ] ตรวจสอบใน Dashboard เห็นเครื่องปรากฏ
- [ ] ทดสอบส่งข้อความ Chat
- [ ] ตรวจสอบ Metrics แสดงผลถูกต้อง
- [ ] ตั้งค่า Auto-start (ถ้าต้องการ)

---

## 🆘 Support & Troubleshooting

### ปัญหาที่พบบ่อย:

1. **Agent เชื่อมต่อไม่ได้:**
   - ตรวจสอบ SERVER_URL ถูกต้องหรือไม่
   - Ping Server: `ping 10.31.112.155`
   - ตรวจสอบ Firewall

2. **Python ไม่ทำงาน:**
   - ติดตั้ง Python และติ๊ก "Add to PATH"
   - รีสตาร์ทเครื่อง

3. **Libraries ติดตั้งไม่ได้:**
   - ตรวจสอบ Internet connection
   - ใช้ `py -m pip install` แทน `pip install`

---

## 📞 ติดต่อ Admin

หากติดตั้งไม่ได้ ติดต่อ:
- Email: admin@company.com
- Tel: xxx-xxx-xxxx
- หรือใช้ระบบ Helpdesk

---

**สร้างโดย: PC Monitor & Chat System**
**เวอร์ชัน: 1.0**
